@{
    ViewBag.Title = "Home Page";
}


<div class="jumbotron">
    <h1>WebAPI and Angular Exposed Affair</h1>
    <p class="lead">
          This is a sample code which may give developers an idea that WebAPI and angular1 can be truly engaged instead of this separation ;)
    <br />We might get more data from WebAPI and process them inside angular, like model's attribute, validation or any kind of back-end model related data.
    </p>
    <p>
    <a href="#" class="btn btn-primary btn-lg" ng-click="GetStudents()" >Get Students List</a><a href="#" class="btn btn-primary btn-lg" ng-click="GetAPI()" >Realod API CACHED</a></p>

</div>

<div class="container">
    <div class="row"><a href="#" class="btn btn-primary btn-lg" ng-click="AddStudent()" style="margin-bottom:10px;">Add New Student</a></div>
    <div class="row">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>#</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Score</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <tr ng-repeat="row in StudentList">
                    <th scope="row">{{row.Id}}</th>
                    <td ng-show="!row.$edit">{{row.FirstName}}</td>
                    <td ng-show="row.$edit"><input type="text" ng-model="row.FirstName" /></td>
                    <td ng-show="!row.$edit">{{row.LastName}}</td>
                    <td ng-show="row.$edit"><input type="text" ng-model="row.LastName" /></td>
                    <td ng-show="!row.$edit">{{row.Score}}</td>
                    <td ng-show="row.$edit"><input type="text" ng-model="row.Score" /></td>
                    <td><a href="#" class="btn btn-default" ng-show="!row.$edit" ng-click="row.$edit=true">Edit</a><a href="#" class="btn btn-danger" ng-show="!row.$edit" ng-click="Delete(row,$index)">Delete</a><a href="#" class="btn btn-primary" ng-show="row.$edit" ng-click="SaveChanges(row)">Save</a><a href="#" class="btn btn-default" ng-show="row.$edit" ng-click="CancelChanges(row,$index)">Cancel</a></td>
                </tr>
            </tbody>
        </table>
    </div>
</div>